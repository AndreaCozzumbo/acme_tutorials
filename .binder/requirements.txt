# Python dependencies for ACME Tutorials
# Optimized for Binder - minimal versions for faster build

# Core scientific computing (let Binder use cached versions)
# Minimal Binder requirements - users install packages as needed
# This keeps Binder startup fast

# Only essential packages - Binder already provides:
# - numpy, scipy, matplotlib, pandas
# - jupyter, notebook

# No pre-installed packages
# Users can install packages in notebooks with:
# !pip install package-name


# Jupyter (Binder provides base, we just ensure notebook support)
notebook<7.0


